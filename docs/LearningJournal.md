## July 12, 2026

Today I learned:

- Flexbox
- Responsive containers
- max-width
- margin: auto
- Box shadows
- Service cards

## July 13, 2026

Today I learned:

- The `<a>` tag creates hyperlinks.
- `href` tells the browser where the link should go.
- `href="page.html"` opens another webpage.
- `href="#section"` jumps to a section on the current page.

- Used Flexbox to build a responsive navigation bar.
- Learned how justify-content: space-between positions elements.
- Learned the difference between styling a parent (.nav-links) and its child elements (.nav-links a).
- Learned how the :hover pseudo-class works.
- Used CSS transitions to animate elements smoothly.
- Used transform: translateY() to create a lifting effect.
- Learned that CSS can style an element differently when the mouse is over it.
- Learned how the :hover pseudo-class changes an element while the mouse is over it.
- Used transition to animate color and movement smoothly.
- Used transform: translateY() to create lift effects.
- Animated a navigation menu, service cards, and a button.
- Learned that HTML creates the content and structure, while CSS controls the layout and appearance.
- Learned that HTML provides the content while CSS controls the layout and presentation.
- Learned how Flexbox can position content in a footer using `justify-content: space-between`.
- Learned that HTML provides the structure, but CSS controls the layout and positioning.
- Learned to organize branding assets separately from website assets.
- Learned that websites reference images from an images folder using relative paths.